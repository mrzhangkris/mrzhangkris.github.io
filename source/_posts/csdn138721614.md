---
title: "创建内存盘"
date: 2024-05-11 16:42:33
categories: [技术]
tags: [Linux]
copyright_author: 张鹏
cover: /images/csdn/covers/csdn138721614.png
---

注入内核模块，一个16GB的内存盘

```bash
modprobe brd rd_nr=1 rd_size=16777216 max_part=0
```

-   rd\_size值为KB

查看创建的内存盘

```bash
ls /dev/ram*
```

磁盘设备格式化

```bash
mkfs.ext4 /dev/ram0
```

创建挂载设备，进行挂载

```bash
mount /dev/ram0 /data
```

卸载设备

```bash
umount /data
umount /dev/ram0
modprobe -r brd
```

开机自启动

```bash
echo "options brd rd_nr=1 rd_size=16777216 max_part=0" >> /etc/modprobe.d/memdisk.conf
echo "mkfs.ext4 /dev/ram0" >> /etc/rc.d/rc.local
echo "mount /dev/ram0 /data1" >> /etc/rc.d/rc.local
```

---

> 本文迁移自作者 CSDN 博客，2024-05-11 首发于 CSDN，内容保持原貌。
